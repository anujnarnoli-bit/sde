services:
  - type: web
    name: sde-role-app
    env: node
    plan: free
    buildCommand: "echo 'No build step required'"
    startCommand: "npm start"
    envVars:
      - key: NODE_VERSION
        value: 20
